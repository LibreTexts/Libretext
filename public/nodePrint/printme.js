const root = "https://dynamic.libretexts.org/";
// https://chem.libretexts.org/Template:Custom/Views/ContentHeader
/*
window.addEventListener("load", () => {
	let printme = document.getElementById("printme");
	printme.href = root + "print/url=" + window.location+".pdf";
});*/ //Directly in content header rather than waiting for load
